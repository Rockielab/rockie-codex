---
name: Bug report
about: Something in rockie doesn't work as documented.
labels: bug
---

**Environment**
- OS: (macOS / Linux distro)
- `sqlite3` version (`/usr/bin/sqlite3 --version`):
- Python version (`python3 --version`):
- rockie commit (`git log -1 --oneline`):

**What did you run?**
<!-- Exact commands. Ideally a mktemp-based reproducer. -->

**What did you expect?**

**What happened?**
<!-- Include relevant lines from .codex/memory/hook.log if applicable. -->

**Does `bash tests/smoke-test.sh` still pass?**
<!-- Yes / No — if No, paste the failing assertion(s). -->
